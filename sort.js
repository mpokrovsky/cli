function sort(flag) {
    switch (flag) {
        case 'importance':
            break;
        case 'user':
            break;
        case 'date':
            break;
        default:
            break;
    }
}

module.exports = {
    sort
}
