function sort(arg) {
    switch (arg) {
        case 'importance':
            break;
        case 'user':
            break;
        case 'date':
            break;
    }
}

module.exports = {
    sort
}
